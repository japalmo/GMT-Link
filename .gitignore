# Dependencias
node_modules/

# Builds
dist/
build/
*.tsbuildinfo

# Entorno — NUNCA commitear secretos
.env
.env.local
.env.*.local

# Logs y temporales
*.log
.DS_Store
Thumbs.db

# Uploads del storage local (dev) — los archivos no se versionan (§9)
nodes/backend-central/var/

# Backups de datos generados por reload-volumes.mjs (dumps de BD, no versionar)
nodes/backend-central/prisma/backups/

# IDE
.vscode/
.idea/

# Claude Code (local)
.claude/settings.local.json

# Datos de runtime / uploads del backend local (91MB) — nunca versionar
/apps/
